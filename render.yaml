services:
  - type: web
    name: ustad-ai-server
    runtime: node
    rootDir: server
    buildCommand: npm install && npm run prebuild && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
